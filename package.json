@import url('https://fonts.googleapis.com/css2?family=Noto+Serif+KR:wght@300;400;500;600&family=Noto+Sans+KR:wght@300;400;500&display=swap');

:root {
  --cream: #FAF7F2;
  --cream-dark: #F0EBE1;
  --brown: #8B6F47;
  --brown-dark: #5C4A32;
  --brown-light: #C4A882;
  --gold: #C9A84C;
  --gold-light: #F0E0A8;
  --forest: #3D6B4F;
  --forest-light: #E8F0EB;
  --text: #2C1F0E;
  --text-mid: #6B5744;
  --text-soft: #9E8878;
  --border: rgba(139,111,71,0.2);
  --border-mid: rgba(139,111,71,0.35);
  --white: #FFFFFF;
  --shadow: 0 1px 3px rgba(44,31,14,0.08), 0 4px 16px rgba(44,31,14,0.06);
  --shadow-lg: 0 8px 32px rgba(44,31,14,0.12);
}

* { box-sizing: border-box; margin: 0; padding: 0; }

html { font-size: 16px; }

body {
  font-family: 'Noto Sans KR', sans-serif;
  background: var(--cream);
  color: var(--text);
  min-height: 100vh;
  line-height: 1.7;
}

/* Typography */
.serif { font-family: 'Noto Serif KR', serif; }

h1, h2, h3 { font-family: 'Noto Serif KR', serif; font-weight: 500; }

/* Cross decoration */
.cross {
  display: inline-block;
  width: 20px; height: 20px;
  position: relative;
}
.cross::before {
  content: '✝';
  font-size: 18px;
  color: var(--brown);
}

/* Layout */
.container {
  max-width: 680px;
  margin: 0 auto;
  padding: 0 1rem;
}

/* Card */
.card {
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 1.5rem;
  margin-bottom: 1rem;
  box-shadow: var(--shadow);
}

/* Inputs */
input[type="text"],
input[type="password"],
input[type="date"],
textarea,
select {
  width: 100%;
  border: 1px solid var(--border-mid);
  border-radius: 8px;
  padding: 10px 14px;
  font-size: 15px;
  font-family: 'Noto Sans KR', sans-serif;
  background: var(--cream);
  color: var(--text);
  outline: none;
  transition: border-color 0.2s, box-shadow 0.2s;
  resize: vertical;
}
input:focus, textarea:focus, select:focus {
  border-color: var(--brown);
  box-shadow: 0 0 0 3px rgba(139,111,71,0.12);
  background: var(--white);
}
textarea { min-height: 90px; line-height: 1.7; }

/* Buttons */
.btn {
  padding: 10px 20px;
  border-radius: 8px;
  font-size: 14px;
  font-family: 'Noto Sans KR', sans-serif;
  font-weight: 500;
  cursor: pointer;
  border: 1px solid var(--border-mid);
  background: transparent;
  color: var(--text-mid);
  transition: all 0.15s;
}
.btn:hover { background: var(--cream-dark); }
.btn:active { transform: scale(0.98); }

.btn-primary {
  background: var(--brown);
  color: var(--white);
  border-color: var(--brown);
}
.btn-primary:hover { background: var(--brown-dark); border-color: var(--brown-dark); }

.btn-pastor {
  background: var(--forest);
  color: var(--white);
  border-color: var(--forest);
}
.btn-pastor:hover { background: #2d5039; }

.btn-gold {
  background: var(--gold);
  color: var(--brown-dark);
  border-color: var(--gold);
}
.btn-gold:hover { background: #b8963e; }

.btn-sm {
  padding: 6px 14px;
  font-size: 13px;
}

.btn-danger {
  color: #8B2020;
  border-color: rgba(139,32,32,0.3);
}
.btn-danger:hover { background: rgba(139,32,32,0.06); }

/* Badge */
.badge {
  display: inline-block;
  padding: 2px 10px;
  border-radius: 100px;
  font-size: 12px;
  font-weight: 500;
}
.badge-pastor { background: var(--forest-light); color: var(--forest); }
.badge-user { background: var(--gold-light); color: var(--brown-dark); }

/* Tabs */
.tabs {
  display: flex;
  border-bottom: 1px solid var(--border);
  margin-bottom: 1.25rem;
}
.tab-item {
  padding: 10px 18px;
  font-size: 14px;
  font-family: 'Noto Sans KR', sans-serif;
  cursor: pointer;
  color: var(--text-soft);
  border-bottom: 2px solid transparent;
  margin-bottom: -1px;
  transition: color 0.15s, border-color 0.15s;
}
.tab-item.active {
  color: var(--brown);
  border-bottom-color: var(--brown);
  font-weight: 500;
}

/* Toast */
.toast-wrap {
  position: fixed;
  bottom: 2rem;
  left: 50%;
  transform: translateX(-50%);
  z-index: 999;
}
.toast {
  background: var(--brown-dark);
  color: var(--white);
  padding: 10px 20px;
  border-radius: 8px;
  font-size: 14px;
  font-family: 'Noto Sans KR', sans-serif;
  white-space: nowrap;
  animation: fadeup 0.3s ease;
}
.toast.error { background: #6B2020; }
@keyframes fadeup {
  from { opacity: 0; transform: translateY(8px); }
  to   { opacity: 1; transform: translateY(0); }
}

/* Section labels */
.section-label {
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--text-soft);
  margin-bottom: 10px;
}

/* QT section */
.qt-block { margin-bottom: 1.25rem; }
.qt-block-title {
  font-size: 14px;
  font-weight: 500;
  color: var(--brown);
  margin-bottom: 6px;
  display: flex;
  align-items: center;
  gap: 8px;
}
.qt-num {
  width: 22px; height: 22px;
  border-radius: 50%;
  border: 1px solid var(--brown-light);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 11px;
  color: var(--brown);
  font-weight: 600;
  flex-shrink: 0;
}

/* Entry cards */
.entry-card {
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 1.25rem;
  margin-bottom: 0.75rem;
  cursor: pointer;
  transition: box-shadow 0.15s, transform 0.15s;
  box-shadow: var(--shadow);
}
.entry-card:hover {
  box-shadow: var(--shadow-lg);
  transform: translateY(-1px);
}
.entry-card-meta {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 6px;
}
.entry-date { font-size: 12px; color: var(--text-soft); }
.entry-book {
  font-family: 'Noto Serif KR', serif;
  font-size: 16px;
  font-weight: 500;
  margin-bottom: 4px;
  color: var(--text);
}
.entry-preview {
  font-size: 13px;
  color: var(--text-soft);
  line-height: 1.5;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

/* Modal */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(44,31,14,0.5);
  z-index: 100;
  display: flex;
  align-items: flex-start;
  justify-content: center;
  padding: 2rem 1rem;
  overflow-y: auto;
}
.modal {
  background: var(--white);
  border-radius: 16px;
  padding: 2rem;
  width: 100%;
  max-width: 600px;
  box-shadow: var(--shadow-lg);
  margin: auto;
}

/* Today's word box */
.word-box {
  background: linear-gradient(135deg, var(--brown-dark) 0%, var(--brown) 100%);
  border-radius: 14px;
  padding: 1.5rem;
  margin-bottom: 1.25rem;
  color: var(--white);
  position: relative;
  overflow: hidden;
}
.word-box::before {
  content: '✝';
  position: absolute;
  right: 1.5rem;
  top: 1rem;
  font-size: 40px;
  opacity: 0.15;
}
.word-box-label {
  font-size: 11px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  opacity: 0.7;
  margin-bottom: 8px;
}
.word-box-verse {
  font-family: 'Noto Serif KR', serif;
  font-size: 15px;
  font-weight: 400;
  line-height: 1.8;
  opacity: 0.95;
  margin-bottom: 10px;
}
.word-box-ref {
  font-size: 13px;
  opacity: 0.75;
  font-weight: 500;
}
.word-box-body {
  font-size: 14px;
  line-height: 1.8;
  opacity: 0.85;
  margin-top: 10px;
  padding-top: 10px;
  border-top: 1px solid rgba(255,255,255,0.2);
}

/* Header */
.site-header {
  background: var(--white);
  border-bottom: 1px solid var(--border);
  padding: 1rem 0;
  margin-bottom: 1.5rem;
  position: sticky;
  top: 0;
  z-index: 50;
}
.site-header-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.site-logo {
  font-family: 'Noto Serif KR', serif;
  font-size: 18px;
  font-weight: 500;
  color: var(--brown-dark);
  display: flex;
  align-items: center;
  gap: 8px;
}

/* User chip */
.user-chip {
  display: flex;
  align-items: center;
  gap: 10px;
  background: var(--cream-dark);
  border-radius: 100px;
  padding: 6px 12px 6px 8px;
  margin-bottom: 1.25rem;
}
.avatar {
  width: 32px; height: 32px;
  border-radius: 50%;
  background: var(--brown-light);
  color: var(--white);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 13px;
  font-weight: 600;
}
.avatar-pastor { background: var(--forest); }

/* Field group */
.field-group { margin-bottom: 1rem; }
.field-label {
  display: block;
  font-size: 13px;
  color: var(--text-mid);
  margin-bottom: 5px;
  font-weight: 500;
}

/* Divider */
.divider {
  display: flex;
  align-items: center;
  gap: 12px;
  color: var(--text-soft);
  font-size: 12px;
  margin: 1rem 0;
}
.divider::before, .divider::after {
  content: '';
  flex: 1;
  height: 1px;
  background: var(--border);
}

/* Empty state */
.empty {
  text-align: center;
  padding: 3rem 1rem;
  color: var(--text-soft);
  font-size: 14px;
}
.empty-icon {
  font-size: 32px;
  margin-bottom: 12px;
  opacity: 0.4;
}

/* Detail section */
.detail-section {
  border-top: 1px solid var(--border);
  padding-top: 1rem;
  margin-top: 1rem;
}
.detail-section-title {
  font-size: 12px;
  font-weight: 500;
  color: var(--brown);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  margin-bottom: 6px;
}
.detail-content {
  font-size: 15px;
  line-height: 1.8;
  color: var(--text);
  white-space: pre-wrap;
}

/* Verse highlight */
.verse-highlight {
  background: var(--gold-light);
  border-left: 3px solid var(--gold);
  padding: 10px 14px;
  border-radius: 0 8px 8px 0;
  font-family: 'Noto Serif KR', serif;
  font-size: 15px;
  line-height: 1.8;
}

.btn-row {
  display: flex;
  gap: 8px;
  justify-content: flex-end;
  margin-top: 1rem;
}

.flex-between {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.gap-2 { gap: 8px; }
.mb-1 { margin-bottom: 0.5rem; }
.mb-2 { margin-bottom: 1rem; }
.text-sm { font-size: 13px; }
.text-soft { color: var(--text-soft); }
